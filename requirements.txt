# Equity Research Lab — Dependências
# Atualizado: 2026-05-17 — versões flexibilizadas para compatibilidade com Python 3.14

# Data analysis
polars>=1.20
duckdb>=1.1
pyarrow>=19.0
pandas>=2.2
numpy>=2.0

# Market data
yfinance>=0.2.50
requests>=2.31

# CLI / utils
tqdm>=4.66
python-dotenv>=1.0

# LLM (opcional — só se usar Anthropic API direto em vez de Cowork)
anthropic>=0.42

# Indicadores técnicos (opcional — fallback nativo existe)
# Se instalar: brew install ta-lib (Mac) ou apt install libta-lib-dev (Linux)
# ta-lib-binary==0.4.32

# Dashboard
streamlit>=1.41
altair>=5.5

# Telegram bot (opcional — semana 8)
# python-telegram-bot==21.10
