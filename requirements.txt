# Equity Research Lab — Pinned dependencies
# Atualizado: 2026-05-17

# Data analysis
polars==1.20.0
duckdb==1.1.3
pyarrow==18.1.0
pandas==2.2.3
numpy==2.2.0

# Market data
yfinance==0.2.51
requests==2.32.3

# CLI / utils
tqdm==4.67.1
python-dotenv==1.0.1

# LLM (opcional — só se usar Anthropic API direto em vez de Cowork)
anthropic==0.42.0

# Indicadores técnicos (opcional — fallback nativo existe)
# Se instalar: brew install ta-lib (Mac) ou apt install libta-lib-dev (Linux)
# ta-lib-binary==0.4.32

# Dashboard (opcional — semana 6)
# streamlit==1.41.1
# altair==5.5.0

# Telegram bot (opcional — semana 8)
# python-telegram-bot==21.10
